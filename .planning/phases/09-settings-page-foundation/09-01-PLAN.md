---
phase: 09-settings-page-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/storage/database.go
  - internal/server/handlers.go
  - internal/server/routes.go
  - templates/pages/settings.gohtml
  - templates/partials/settings-page.gohtml
  - templates/partials/sidebar.gohtml
  - static/styles.css
autonomous: false

must_haves:
  truths:
    - "User sees gear icon in sidebar navigation"
    - "User can click gear icon and settings page loads"
    - "Settings page displays all tracked blogs"
    - "Each blog shows name, URL, and article count"
    - "Settings page works via HTMX swap and direct URL access"
  artifacts:
    - path: "internal/storage/database.go"
      provides: "ListBlogsWithCounts method with LEFT JOIN query"
      contains: "BlogWithCount"
    - path: "internal/server/handlers.go"
      provides: "handleSettings with HTMX detection"
      contains: "handleSettings"
    - path: "templates/pages/settings.gohtml"
      provides: "Full settings page template"
      min_lines: 15
    - path: "templates/partials/settings-page.gohtml"
      provides: "Settings content partial for HTMX swap"
      min_lines: 20
    - path: "templates/partials/sidebar.gohtml"
      provides: "Settings gear icon link"
      contains: "settings"
  key_links:
    - from: "templates/partials/sidebar.gohtml"
      to: "/settings"
      via: "HTMX navigation link"
      pattern: 'hx-get="/settings"'
    - from: "internal/server/handlers.go"
      to: "internal/storage/database.go"
      via: "ListBlogsWithCounts method call"
      pattern: "db.ListBlogsWithCounts"
    - from: "internal/server/handlers.go"
      to: "templates/partials/settings-page.gohtml"
      via: "Template rendering"
      pattern: 'renderTemplate.*settings-page.gohtml'
---

<objective>
Create settings page foundation for BlogWatcher UI where users can view all tracked blogs with their metadata.

Purpose: Establish settings infrastructure for v1.2 blog management features. Provides the UI foundation for future add/edit/remove blog functionality.

Output: Working settings page accessible from sidebar showing all blogs with article counts, using existing HTMX + Go templates architecture.
</objective>

<execution_context>
@/Users/esteban.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/esteban.Torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-settings-page-foundation/09-RESEARCH.md
@internal/server/handlers.go
@internal/storage/database.go
@templates/partials/sidebar.gohtml
</context>

<tasks>

<task type="auto">
  <name>Add database method for blogs with article counts</name>
  <files>
    internal/storage/database.go
  </files>
  <action>
Add `BlogWithCount` struct and `ListBlogsWithCounts()` method to Database type.

**BlogWithCount struct:**
```go
type BlogWithCount struct {
    model.Blog
    ArticleCount int
}
```

**ListBlogsWithCounts() method:**
Use LEFT JOIN with GROUP BY to get all blogs with their article counts in a single query:
```sql
SELECT
    b.id,
    b.name,
    b.url,
    b.feed_url,
    b.scrape_selector,
    b.last_scanned,
    COUNT(a.id) as article_count
FROM blogs b
LEFT JOIN articles a ON b.id = a.blog_id
GROUP BY b.id, b.name, b.url, b.feed_url, b.scrape_selector, b.last_scanned
ORDER BY b.name
```

Follow existing codebase pattern from ListBlogs():
- Use sql.NullString for nullable columns (feed_url, scrape_selector, last_scanned)
- Parse last_scanned timestamp using parseTime() helper
- Return []BlogWithCount and error
- Handle rows.Scan() for all columns including article_count int

Place method after ListBlogs() method for logical grouping.
  </action>
  <verify>
Run `go build ./...` to verify compilation.
Search codebase to confirm BlogWithCount struct and ListBlogsWithCounts method exist:
```
grep -n "type BlogWithCount" internal/storage/database.go
grep -n "func.*ListBlogsWithCounts" internal/storage/database.go
```
  </verify>
  <done>
Database has BlogWithCount struct and ListBlogsWithCounts() method that returns all blogs with article counts using LEFT JOIN + GROUP BY. Code compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Create settings handler, route, and templates</name>
  <files>
    internal/server/handlers.go
    internal/server/routes.go
    templates/pages/settings.gohtml
    templates/partials/settings-page.gohtml
    templates/partials/sidebar.gohtml
    static/styles.css
  </files>
  <action>
**1. Add handleSettings to handlers.go:**

Follow exact pattern from handleArticleList (lines 73-133) with HTMX header detection:
```go
func (s *Server) handleSettings(w http.ResponseWriter, r *http.Request) {
    blogs, err := s.db.ListBlogsWithCounts()
    if err != nil {
        log.Printf("Error fetching blogs with counts: %v", err)
        http.Error(w, "Database error", http.StatusInternalServerError)
        return
    }

    data := map[string]interface{}{
        "Blogs":          blogs,
        "IsSettingsPage": true,
    }

    // Check if this is an HTMX request
    if r.Header.Get("HX-Request") == "true" {
        // Return partial fragment for HTMX
        s.renderTemplate(w, "settings-page.gohtml", data)
        return
    }

    // Return full page for direct navigation
    data["Title"] = "Settings - BlogWatcher"
    data["Version"] = s.version
    s.renderTemplate(w, "settings.gohtml", data)
}
```

**2. Add route to routes.go:**
Add after existing article/blog routes:
```go
s.mux.HandleFunc("GET /settings", s.handleSettings)
```

**3. Create templates/pages/settings.gohtml:**
Follow exact structure from index.gohtml:
```html
{{define "settings.gohtml"}}
{{template "base" .}}
{{end}}

{{define "title"}}Settings - BlogWatcher{{end}}

{{define "content"}}
<div class="app-layout">
    {{template "sidebar.gohtml" .}}

    <main id="main-content" class="main-content">
        {{template "settings-page.gohtml" .}}
    </main>
</div>
{{end}}
```

**4. Create templates/partials/settings-page.gohtml:**
```html
{{define "settings-page.gohtml"}}
<div class="main-content-header">
    <header class="main-header">
        <h1>Settings</h1>
    </header>
</div>
<div class="main-content-body">
    <section class="settings-section">
        <h2>Tracked Blogs</h2>
        {{if .Blogs}}
        <div class="blog-settings-list">
            {{range .Blogs}}
            <div class="blog-settings-card">
                <div class="blog-settings-info">
                    <h3 class="blog-settings-name">{{.Name}}</h3>
                    <a href="{{.URL}}" target="_blank" rel="noopener noreferrer"
                       class="blog-settings-url">{{.URL}}</a>
                    <div class="blog-settings-meta">
                        <span class="article-count">{{.ArticleCount}} article{{if ne .ArticleCount 1}}s{{end}}</span>
                    </div>
                </div>
            </div>
            {{end}}
        </div>
        {{else}}
        <p class="empty-state">No blogs tracked yet.</p>
        {{end}}
    </section>
</div>
{{end}}
```

**5. Update templates/partials/sidebar.gohtml:**
Add settings link after "Archived" link (line 40), before subscriptions section (line 43).

Use Feather Icons settings/gear SVG with same attributes as existing icons (width="18" height="18", stroke-width="2", stroke="currentColor"):
```html
<a href="/settings"
   hx-get="/settings"
   hx-target="#main-content"
   hx-push-url="true"
   hx-on::after-swap="document.getElementById('sidebar-toggle').checked = false"
   class="nav-link{{if .IsSettingsPage}} active{{end}}">
    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="3"></circle>
        <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path>
    </svg>
    Settings
</a>
```

**6. Add CSS to static/styles.css:**
Append settings styles following existing component patterns (using CSS custom properties):
```css
/* Settings Page Styles */
.settings-section {
    padding: 2rem;
}

.settings-section h2 {
    font-size: 1.25rem;
    color: var(--text-secondary);
    font-weight: 500;
    margin-bottom: 1.5rem;
}

.blog-settings-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.blog-settings-card {
    background-color: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1.5rem;
    transition: background-color 0.2s ease;
}

.blog-settings-card:hover {
    background-color: var(--bg-elevated);
}

.blog-settings-info {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.blog-settings-name {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0;
}

.blog-settings-url {
    color: var(--accent);
    font-size: 0.875rem;
    word-break: break-all;
    text-decoration: none;
}

.blog-settings-url:hover {
    text-decoration: underline;
}

.blog-settings-meta {
    display: flex;
    gap: 1rem;
    font-size: 0.875rem;
    color: var(--text-secondary);
}

.article-count {
    display: flex;
    align-items: center;
    gap: 0.25rem;
}

/* Mobile responsive */
@media (max-width: 768px) {
    .settings-section {
        padding: 1rem;
    }
}
```
  </action>
  <verify>
Build and run server:
```
go build -o blogwatcher-ui ./cmd/server
./blogwatcher-ui
```

Verify files exist:
```
ls templates/pages/settings.gohtml
ls templates/partials/settings-page.gohtml
grep -n "GET /settings" internal/server/routes.go
grep -n "handleSettings" internal/server/handlers.go
grep -n "settings" templates/partials/sidebar.gohtml
grep -n "settings-section" static/styles.css
```

Check HTMX detection pattern matches existing handlers:
```
grep -A2 'HX-Request.*true' internal/server/handlers.go
```
  </verify>
  <done>
Settings route registered, handler created with HTMX detection, templates created following index.gohtml pattern, gear icon added to sidebar with proper HTMX attributes, CSS styles added. Server compiles and runs without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Settings page accessible from sidebar with blog list display and article counts
  </what-built>
  <how-to-verify>
1. Start server and navigate to http://localhost:8080
2. Verify gear icon appears in sidebar navigation (between "Archived" and "Subscriptions")
3. Click gear icon - settings page should load via HTMX swap without full page reload
4. Verify settings page displays:
   - "Settings" header
   - "Tracked Blogs" section
   - List of all blogs from database
   - Each blog shows: name, URL (as clickable link), article count
5. Click a blog URL - should open in new tab
6. Test direct URL access: Navigate to http://localhost:8080/settings directly
   - Page should load with full layout (sidebar + settings content)
7. Test browser back button - should navigate back to articles view
8. Test mobile view (resize browser to <768px):
   - Settings gear icon should be visible in sidebar
   - Settings page content should have reduced padding
   - Blog cards should be readable
9. Test empty state (if database has no blogs):
   - Should display "No blogs tracked yet." message
10. Verify active state: Settings link in sidebar should have "active" class when on settings page
  </how-to-verify>
  <resume-signal>
Type "approved" if settings page works correctly, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
**Database layer verification:**
- ListBlogsWithCounts() method exists and uses LEFT JOIN + GROUP BY
- BlogWithCount struct includes model.Blog and ArticleCount field
- Query returns all blogs with correct article counts (including 0 for blogs with no articles)

**Handler verification:**
- handleSettings follows existing HTMX detection pattern
- Partial template returned for HTMX requests (HX-Request: true)
- Full page returned for direct URL access
- IsSettingsPage flag passed to templates for active state

**Template verification:**
- settings.gohtml follows index.gohtml structure (base template + sidebar + main-content)
- settings-page.gohtml displays blog list with metadata
- Gear icon in sidebar matches Feather Icons style (18x18, stroke-width 2)
- HTMX attributes on settings link match existing nav links

**CSS verification:**
- Settings styles use CSS custom properties (--text-primary, --bg-surface, etc.)
- Mobile responsive (@media max-width: 768px)
- Hover states on cards
</verification>

<success_criteria>
1. User sees gear icon in sidebar navigation matching existing icon style
2. Clicking gear icon loads settings page via HTMX swap (no full reload)
3. Settings page displays all blogs from database with names, URLs, and article counts
4. Direct URL access to /settings works (full page with sidebar)
5. Browser history navigation works (back/forward buttons)
6. Active state highlights settings link when on settings page
7. Mobile layout works (responsive padding, readable cards)
8. Empty state displays when no blogs tracked
9. All code compiles without errors
10. All three phase requirements satisfied:
    - SETT-01: Gear icon in sidebar navigates to settings
    - SETT-02: Settings page displays all blogs
    - SETT-03: Blog entries show name, URL, article count
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-page-foundation/09-01-SUMMARY.md`
</output>
