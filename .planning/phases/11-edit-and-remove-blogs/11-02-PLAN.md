---
phase: 11-edit-and-remove-blogs
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - internal/storage/database.go
autonomous: true

must_haves:
  truths:
    - "articles.blog_id column accepts NULL values"
    - "Existing articles remain linked to their blogs (no data loss)"
    - "Foreign key constraint still enforced (valid IDs must reference blogs table)"
    - "Orphaned articles (NULL blog_id) can be queried separately if needed"
  artifacts:
    - path: "internal/storage/database.go"
      provides: "Migration to make articles.blog_id nullable"
      contains: "ALTER TABLE articles"
  key_links:
    - from: "ensureMigrations"
      to: "articles table schema"
      via: "ALTER TABLE if column not nullable"
      pattern: "ALTER TABLE articles.*blog_id"
---

<objective>
Migrate database schema to allow articles.blog_id to be NULL, enabling blog deletion without article cascade.

Purpose: Prepare database for user's deletion choice - "Delete blog only" will set articles.blog_id to NULL, "Delete blog + articles" will remove both.
Output: Schema migration that runs idempotently on application startup without data loss.
</objective>

<execution_context>
@/Users/esteban.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/esteban.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/PROJECT.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/ROADMAP.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/STATE.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/internal/storage/database.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema migration for nullable blog_id</name>
  <files>internal/storage/database.go</files>
  <action>
Add migration logic to ensureMigrations method (after FTS5 setup, around line 122):

**SQLite constraint:** SQLite does not support ALTER COLUMN to change NOT NULL constraint. Must recreate table with new schema.

**Migration steps** (wrapped in a single transaction):

1. Check if migration needed:
   - Query PRAGMA table_info(articles)
   - Parse notnull field for blog_id column
   - If blog_id already allows NULL (notnull=0), skip migration

2. If migration needed, execute in transaction:
   - BEGIN TRANSACTION
   - CREATE TABLE articles_new with same schema as articles but blog_id without NOT NULL
   - Schema: id INTEGER PRIMARY KEY, blog_id INTEGER (nullable), title TEXT NOT NULL, url TEXT NOT NULL UNIQUE, thumbnail_url TEXT, published_date TEXT, discovered_date TEXT, is_read INTEGER NOT NULL DEFAULT 0
   - Foreign key: FOREIGN KEY (blog_id) REFERENCES blogs(id) (still enforce valid IDs, but allow NULL)
   - Copy data: INSERT INTO articles_new SELECT * FROM articles
   - Drop triggers: DROP TRIGGER IF EXISTS articles_ai/au/ad
   - Drop old table: DROP TABLE articles
   - Rename: ALTER TABLE articles_new TO articles
   - Recreate FTS5 table and triggers (same logic as existing ensureMigrations FTS5 setup)
   - COMMIT

**Error handling:**
- If any step fails, ROLLBACK transaction
- Return error with context: "failed to migrate articles.blog_id to nullable: %w"

**Idempotency:**
- Migration only runs if blog_id column currently has NOT NULL constraint
- Safe to run multiple times (checks before executing)
- Existing data preserved (full table copy)

Add this migration BEFORE the FTS5 migration in ensureMigrations (line ~82), since FTS5 setup will run after table recreation to ensure triggers are correct.
  </action>
  <verify>
1. Build succeeds: `go build -o blogwatcher-ui ./cmd/server`
2. Run migration: Start server `./blogwatcher-ui` (migration runs on startup)
3. Check schema: `sqlite3 ~/.blogwatcher/blogwatcher.db "PRAGMA table_info(articles);" | grep blog_id`
4. Verify blog_id shows notnull=0 (nullable)
5. Verify existing articles still exist: `sqlite3 ~/.blogwatcher/blogwatcher.db "SELECT COUNT(*) FROM articles;"`
6. Verify foreign key still works: Try inserting article with invalid blog_id (should fail)
  </verify>
  <done>
Migration exists in ensureMigrations
articles.blog_id accepts NULL values
Existing articles data preserved (no data loss)
Foreign key constraint still enforced for non-NULL values
  </done>
</task>

</tasks>

<verification>
1. Stop server if running
2. Backup database: `cp ~/.blogwatcher/blogwatcher.db ~/.blogwatcher/blogwatcher.db.backup`
3. Start server: `./blogwatcher-ui`
4. Check logs for migration success (no errors)
5. Verify schema: `sqlite3 ~/.blogwatcher/blogwatcher.db "PRAGMA table_info(articles);" | grep -A1 blog_id`
6. Expected: blog_id row with notnull=0 (nullable)
7. Verify data integrity: `sqlite3 ~/.blogwatcher/blogwatcher.db "SELECT COUNT(*) FROM articles;"`
8. Compare count with backup to confirm no data loss
9. Test foreign key: `sqlite3 ~/.blogwatcher/blogwatcher.db "INSERT INTO articles (blog_id, title, url, is_read) VALUES (99999, 'Test', 'http://test.com', 0);"`
10. Expected: Foreign key constraint error (blog_id 99999 doesn't exist)
11. Test NULL insertion: `sqlite3 ~/.blogwatcher/blogwatcher.db "INSERT INTO articles (blog_id, title, url, is_read) VALUES (NULL, 'Orphan', 'http://orphan.com', 0);"`
12. Expected: Success (NULL is now allowed)
13. Clean up test data: `sqlite3 ~/.blogwatcher/blogwatcher.db "DELETE FROM articles WHERE url = 'http://orphan.com';"`
</verification>

<success_criteria>
1. Migration runs idempotently without errors on server startup
2. articles.blog_id column accepts NULL values (notnull=0)
3. All existing articles preserved (no data loss during migration)
4. Foreign key constraint still enforced (invalid blog_id references rejected)
5. NULL blog_id insertions succeed (orphaned articles allowed)
6. FTS5 triggers recreated correctly after table recreation
7. Database remains consistent and usable
</success_criteria>

<output>
After completion, create `.planning/phases/11-edit-and-remove-blogs/11-02-SUMMARY.md`
</output>
