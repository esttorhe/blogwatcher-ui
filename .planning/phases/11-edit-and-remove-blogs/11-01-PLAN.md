---
phase: 11-edit-and-remove-blogs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/storage/database.go
  - internal/server/handlers.go
  - internal/server/routes.go
  - assets/templates/partials/settings-page.gohtml
  - assets/static/styles.css
autonomous: true

must_haves:
  truths:
    - "User sees 'Edit' action next to each blog in settings list"
    - "User can click edit and name becomes editable input field"
    - "User can save new blog name and it persists to database"
    - "Changed blog name updates in sidebar subscriptions list"
    - "User can cancel edit without saving changes"
  artifacts:
    - path: "internal/storage/database.go"
      provides: "GetBlogByID and UpdateBlogName methods"
      exports: ["GetBlogByID", "UpdateBlogName"]
    - path: "internal/server/handlers.go"
      provides: "handleEditBlog and handleUpdateBlogName handlers"
      min_lines: 40
    - path: "assets/templates/partials/settings-page.gohtml"
      provides: "Inline edit UI with HTMX swap"
      contains: "hx-get=\"/blogs/"
  key_links:
    - from: "assets/templates/partials/settings-page.gohtml"
      to: "/blogs/{id}/edit"
      via: "hx-get on Edit button"
      pattern: "hx-get=\"/blogs/\\{\\{.ID\\}\\}/edit\""
    - from: "internal/server/handlers.go"
      to: "storage.UpdateBlogName"
      via: "PUT handler updates database"
      pattern: "db\\.UpdateBlogName"
---

<objective>
Implement inline blog name editing with HTMX click-to-edit pattern.

Purpose: Allow users to rename blogs directly from settings page without separate modal or navigation.
Output: Working edit functionality with database persistence and sidebar auto-refresh.
</objective>

<execution_context>
@/Users/esteban.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/esteban.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/PROJECT.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/ROADMAP.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/STATE.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/.planning/phases/10-add-blog-flow/10-01-SUMMARY.md
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/internal/storage/database.go
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/internal/server/handlers.go
@/Users/esteban.torres/workspace/github/esttorhe/blogwatcher-ui/assets/templates/partials/settings-page.gohtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add database methods for blog editing</name>
  <files>internal/storage/database.go</files>
  <action>
Add two new methods to Database struct in database.go:

1. **GetBlogByID(id int64) (*model.Blog, error)**
   - Query: `SELECT id, name, url, feed_url, scrape_selector, last_scanned FROM blogs WHERE id = ?`
   - Use existing scanBlog helper function
   - Return nil if blog not found (not an error)

2. **UpdateBlogName(id int64, name string) error**
   - Query: `UPDATE blogs SET name = ? WHERE id = ?`
   - Execute update, return error if any
   - Simple focused update (not using UpdateBlog which requires full Blog struct)

Add these methods after the existing GetBlogByName method (around line 480) to maintain logical grouping of blog-related database operations.
  </action>
  <verify>
Build succeeds: `go build -o blogwatcher-ui ./cmd/server`
Methods exist in database.go with correct signatures
  </verify>
  <done>
GetBlogByID and UpdateBlogName methods exist and compile successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HTMX edit handlers</name>
  <files>internal/server/handlers.go, internal/server/routes.go</files>
  <action>
In handlers.go, add two handlers after handleAddBlog (around line 500):

1. **handleEditBlog(w http.ResponseWriter, r *http.Request)**
   - Extract blog ID from URL path: `/{id}/edit`
   - Parse ID using strconv.ParseInt
   - Call db.GetBlogByID(id)
   - Handle errors: 404 if blog not found, 500 for database errors
   - Execute template "blog-edit-form.gohtml" with blog data
   - Template data: struct with Blog field
   - Returns HTML form partial (HTMX swap target)

2. **handleUpdateBlogName(w http.ResponseWriter, r *http.Request)**
   - Extract blog ID from URL path
   - Parse form: r.ParseForm()
   - Get new name: r.FormValue("name")
   - Validate: name must be 1-100 chars (return 400 if invalid)
   - Call db.UpdateBlogName(id, name)
   - On success: query updated blog with GetBlogByID
   - Execute template "blog-display-row.gohtml" with updated blog data
   - Add response header: `HX-Trigger: blogListUpdated` (triggers sidebar refresh)
   - Returns HTML blog card partial (replaces edit form)

In routes.go, add two routes in registerRoutes after POST /blogs/add:
- `GET /blogs/{id}/edit` -> handleEditBlog
- `PUT /blogs/{id}` -> handleUpdateBlogName

Use existing pattern from settings route (line 23) for path parameters.
  </action>
  <verify>
Build succeeds: `go build -o blogwatcher-ui ./cmd/server`
Routes registered in routes.go
Handlers exist in handlers.go with correct HTTP methods
  </verify>
  <done>
Edit and update handlers exist with validation and HTMX response headers
Routes mapped correctly to handlers
  </done>
</task>

<task type="auto">
  <name>Task 3: Create inline edit templates and UI</name>
  <files>
assets/templates/partials/settings-page.gohtml
assets/templates/partials/blog-edit-form.gohtml
assets/templates/partials/blog-display-row.gohtml
assets/static/styles.css
  </files>
  <action>
1. **Create blog-display-row.gohtml** (new file):
   - Define template with blog card display (extract from settings-page.gohtml)
   - Wrapper div with class="blog-settings-card" and id="blog-{{.Blog.ID}}"
   - Display: name (h3), URL (link), article count
   - Add button: "Edit" with hx-get="/blogs/{{.Blog.ID}}/edit" hx-target="#blog-{{.Blog.ID}}" hx-swap="outerHTML"
   - Template data expects: .Blog (model.Blog) and .ArticleCount (int)

2. **Create blog-edit-form.gohtml** (new file):
   - Define template with edit form (same wrapper structure as display row)
   - Wrapper div with same id="blog-{{.Blog.ID}}" for HTMX swap
   - Form with hx-put="/blogs/{{.Blog.ID}}" hx-target="#blog-{{.Blog.ID}}" hx-swap="outerHTML"
   - Input field: name="name" value="{{.Blog.Name}}" (pre-filled with current name)
   - Two buttons: "Save" (type="submit") and "Cancel" (hx-get="/blogs/{{.Blog.ID}}/edit" with cancel class)
   - Template data expects: .Blog (model.Blog)

3. **Update settings-page.gohtml**:
   - Replace hardcoded blog card HTML (lines 15-24) with template include
   - Change to: `{{template "blog-display-row.gohtml" (dict "Blog" . "ArticleCount" .ArticleCount)}}`
   - Keep range loop structure intact

4. **Add CSS styles to styles.css**:
   - `.blog-settings-card` - Add position: relative for proper button positioning
   - `.blog-edit-form` - Inline form styling (flex layout, gap between input and buttons)
   - `.blog-edit-form input[type="text"]` - Match existing form-group input styles
   - `.blog-edit-form button` - Inline button styles (margin for spacing)
   - `.blog-action-buttons` - Container for Edit/Remove buttons (display: flex, gap)
   - Button hover states and focus outlines for accessibility

Add CSS after existing .blog-settings-list rules (around line 350).

Pattern: Click Edit → GET /blogs/{id}/edit returns form → User edits → PUT /blogs/{id} saves → Returns display row → HX-Trigger refreshes sidebar.
  </action>
  <verify>
Templates created in assets/templates/partials/
CSS added to styles.css
Build succeeds and templates referenced correctly
  </verify>
  <done>
Inline edit UI exists with HTMX click-to-edit pattern
Edit/Cancel buttons functional
Save triggers database update and sidebar refresh
  </done>
</task>

</tasks>

<verification>
1. Start server: `./blogwatcher-ui`
2. Navigate to Settings page
3. Click "Edit" button next to any blog
4. Verify: Edit form appears inline (name becomes input field)
5. Change blog name and click "Save"
6. Verify: New name persists (displays in settings card)
7. Check sidebar: Blog name updated in subscriptions list
8. Try editing another blog and click "Cancel"
9. Verify: Form closes without saving changes
10. Check database: `sqlite3 ~/.blogwatcher/blogwatcher.db "SELECT name FROM blogs;"`
</verification>

<success_criteria>
1. Edit button appears next to each blog in settings list
2. Clicking edit replaces display with inline form via HTMX
3. Form pre-populates with current blog name
4. Saving new name persists to database (GetBlogByID + UpdateBlogName)
5. After save, display row re-appears with updated name (HTMX swap)
6. Sidebar refreshes automatically (HX-Trigger: blogListUpdated)
7. Cancel button restores display without saving
8. All HTMX interactions work without full page reload
</success_criteria>

<output>
After completion, create `.planning/phases/11-edit-and-remove-blogs/11-01-SUMMARY.md`
</output>
